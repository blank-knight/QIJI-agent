# 知识库初始化脚本

首次使用时创建目录结构和索引文件。
也可以在 GEO 诊断前检查知识库是否就绪。

使用方法：
  python3 scripts/init_knowledge_base.py          # 初始化
  python3 scripts/init_knowledge_base.py --check   # 检查状态
  python3 scripts/init_knowledge_base.py --search "关键词"  # 搜索
